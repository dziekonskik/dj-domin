import { Navigation } from "../navigation";

export const Header = () => {
  return (
    <header className="sm:grid justify-end max-w-7xl container m-auto">
      <Navigation />
    </header>
  );
};
